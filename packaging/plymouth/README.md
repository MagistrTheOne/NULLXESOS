# NULLXES Plymouth Theme

Generated by `theme-export plymouth --out /usr/share/plymouth/themes/nullxes/`.

Files:
- `nullxes.plymouth` — theme manifest
- `nullxes.script` — Plymouth script (deterministic output)
- `logo.png` — generated from theme tokens (96×96, ARGB)

`SOURCE_DATE_EPOCH` is honoured during `theme-export` so reproducible builds
produce byte-identical PNGs given the same `theme.toml`.
