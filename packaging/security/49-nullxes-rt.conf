# /etc/security/limits.d/49-nullxes-rt.conf — real-time priorities for audio.
# `realtime` group is created post-install; users in it can request RT scheduling.

@realtime  -  rtprio  95
@realtime  -  memlock unlimited
@realtime  -  nice    -19
